const {RoomObjectType} = require('lucid-engine/World');

module.exports = LimboRoom = RoomObjectType({
    key: 'limbo',
    title: 'In Limbo',
    description: 'You see nothing but blackness.',
    shortDescription: 'You see nothing but blackness.'
});
